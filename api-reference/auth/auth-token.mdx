---
title: "Get Authentication Token"
description: "Exchange consumer credentials for a short-lived Bearer token."
openapi: "POST /auth/api/v1/generate-token"
---

## Overview

Use this endpoint to exchange your `consumer_key` and `consumer_secret` for a short-lived Bearer token.

This is a server-to-server flow and does **not** require an existing Bearer token.

## Environments

| Environment | Base URL |
| --- | --- |
| Sandbox | `https://dev.waftpay.io` |
| Production | `https://prod.waftpay.io` |

## Endpoint

```http
POST /auth/api/v1/generate-token
Example request
curl --request POST \
  --url https://dev.waftpay.io/auth/api/v1/generate-token \
  --header 'Content-Type: application/json' \
  --data '{
    "consumer_key": "APP_CONSUMER_KEY",
    "consumer_secret": "APP_CONSUMER_SECRET"
  }'
Request body
{
  "consumer_key": "APP_CONSUMER_KEY",
  "consumer_secret": "APP_CONSUMER_SECRET"
}
Example response
{
  "status": true,
  "message": "Request processed successfully",
  "token": "abcdeFGHIjklmno@1234567890...",
  "expires_in": 7200000
}

Use the returned token in the Authorization header:

Authorization: Bearer <token>
Notes
Use credentials for the specific product you are calling: Collections, Payouts, Remittance, or Checkout.
Sandbox credentials work only on https://dev.waftpay.io.
Production credentials work only on https://prod.waftpay.io.
If you regenerate your keys, the old credentials stop working immediately.
 
```